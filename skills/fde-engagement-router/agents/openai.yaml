interface:
  display_name: "FDE 工作流路由"
  short_description: "判断 FDE 当前阶段并推荐对应 skill"
  default_prompt: "Use $fde-engagement-router to decide which FDE skill should handle this customer or consulting request."
policy:
  allow_implicit_invocation: true
