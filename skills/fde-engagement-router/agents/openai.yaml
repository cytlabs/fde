interface:
  display_name: "FDE 工作流路由"
  short_description: "判断 FDE 当前阶段并推荐对应 skill"
  default_prompt: "Use $fde-engagement-router to select exactly one next FDE skill and prepare its handoff context."
policy:
  allow_implicit_invocation: true
