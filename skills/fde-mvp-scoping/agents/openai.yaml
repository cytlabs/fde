interface:
  display_name: "FDE MVP 收敛"
  short_description: "收敛 1-2 周可验证 MVP 闭环"
  default_prompt: "Use $fde-mvp-scoping to narrow a validated AI opportunity into a practical MVP scope."
policy:
  allow_implicit_invocation: true
